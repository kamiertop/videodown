import type {JSXElement} from "solid-js";

export default function NoCover(): JSXElement {
  return (
    <div class="flex h-full items-center justify-center text-xs text-base-content/40">
      无封面
    </div>
  );
}
